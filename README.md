# RockPaperScissors
```
just open Index.html and have fun

apparently CORS policy does not accept ES6 modules from the file system 
or cross-origin without a CORS header so the scripts need to run from a (local) server
```
